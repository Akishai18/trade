[project]
name = "green-api"
version = "0.1.0"
description = "FastAPI backend — the authoritative brain. Submit strategies, run the overfit gate (sandboxed), stream progress over WebSocket, fetch verdicts."
requires-python = ">=3.12"
dependencies = [
    "green-core",
    "green-adapters",
    "green-strategies",
    "green-sandbox",
    "green-generator",
    "fastapi>=0.115",
    "psycopg[binary]>=3.2",
    "psycopg-pool>=3.2",
    "PyJWT[crypto]>=2.10",
    "uvicorn[standard]>=0.30",  # [standard] bundles websockets — required for the WS endpoint
    "pydantic>=2.10",
    "mlflow-skinny>=2.16",  # tracking client only (no server/UI) for run logging
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/green"]
